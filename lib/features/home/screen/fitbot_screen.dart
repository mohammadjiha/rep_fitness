import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class FitBotScreen extends StatelessWidget {
  const FitBotScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("FIT BOT SCREEN", style: TextStyle(color: Colors.white, fontSize: 24)),
    );
  }
}